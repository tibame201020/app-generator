---
title: "🔌 擴充與外掛指南"
subtitle: "如何接入不同的 Worker Agent"
---

## 解耦的藝術：Worker-Agnostic Prompt

本框架的設計哲學是 **Worker-Agnostic（工人無關性）**。Task Dispatcher 產出的 Worker Prompt 對所有 LLM Agent 通用——不管你用的是 Jules、Devin、Cursor Agent 還是自研的 CorpBot，只要它能操作 Git 和執行程式碼，就能接入這座工廠。

### 接入方式

| Worker | 觸發方式 | 說明 |
|:-----------------------|:-----------------------|:-----------------------|
| **Jules** | Web GUI / API | 直接將 Worker Prompt 貼入 Jules 介面或透過 API 傳送 |
| **Devin** | Web GUI / API | 同上，適用 Devin 的任務建立介面 |
| **Cursor Agent** | IDE Prompt | 在 Cursor 中開啟專案目錄，將 Prompt 送入 Agent 模式 |
| **自研 Agent** | Any | 只要能執行 `git`, `gh` CLI 並遵循 AGENT_PROTOCOL.md 即可 |

### 若使用者需要自動化觸發

Task Dispatcher 可協助產出專屬的 API 觸發腳本。詳見 [Task Dispatcher SKILL.md](../skills/task-dispatcher/SKILL.md) 的 Step 3。

------------------------------------------------------------------------

## 🔮 未來：分散式 Worker 管理

::: callout-note
#### 規劃中的功能 (Future Feature)

**分散式 Worker 調度 (Distributed Worker Scheduling)** 是規劃中的未來功能，不包含在目前版本中。

目前框架以**單一 Worker 串行迭代**運作。所有調度邏輯已抽象至 Task Dispatcher，未來擴充時只需將 Dispatcher 從 `mode: instructor`（一次性教導）升級為 `mode: live`（持續性調度），其餘 5 個 Skill 完全不受影響。

可能的擴充方向： - 多 Worker 並行執行不同 Phase 的任務 - Worker 負載均衡與健康監控 - 分散式互斥鎖升級（從 Branch-as-Lock 到 Advisory Lock）
:::