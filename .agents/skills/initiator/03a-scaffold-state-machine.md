---
name: Initiator - Step 3a (Scaffold State Machine & Specs)
description: 將推演出的任務清單，實體化為 Git-as-State-Machine 的狀態機與規格防爆網。
---

# 🏭 Initiator 階段 3a：建置狀態機與規格書

在建置工廠前，**您必須先向使用者確認以下環境變數**：
- `{{AGENT_NAME}}`：該工廠將配置的 AI 工人名稱 (例如 `jules`, `dev-bot` 等)。
- `{{BASE_BRANCH}}`：專案的主要開發分支 (例如 `feature/ai-factory`, `main` 等)。
- `{{BOT_USERNAME}}`：AI 機器人在 Version Control 平台上的帳號名稱。

確認完畢後，請為專案生成前兩項核心藍圖：

1. **`.{{AGENT_NAME}}/tracker.json` (狀態機)**：參考下方範本格式，將推演出的所有任務寫入，預設狀態設為 `pending`。
2. **`specs/phase_X.yml` (規格防爆網)**：建立 `specs/` 目錄，並參考下方格式，為每個階段建立真實的 YAML 規格。

> 🛑 **暫停點 (Checkpoint)**：生成完第一二個檔案後，請自主切換並閱讀 `.agents/skills/initiator/03b-scaffold-agent-protocol.md` 以進行下一步。

---

## 🏗️ 核心架構範本

### 1. `tracker.json` 基本骨架參考
```json
{
  "project": "您的專案名稱",
  "current_phase": "Phase 1: Setup",
  "phases": [
    {
      "phase_id": "phase_1",
      "name": "Phase 1: Setup",
      "tasks": [
        // 填入您設計的 tasks
      ]
    }
  ]
}
```

### 2. `specs/*.yml` 格式範本參考
```yaml
id: "phase_1"
name: "Phase 1: Setup"
description: "在此填寫階段描述，例如：建置底層架構與核心模組"
objectives:
  - "建立專案骨架"
modules:
  - id: "1.1"
    name: "專案初始化"
    tasks:
      - id: "task_1_1_1" # 必須與 tracker.json 中的 ID 完全一致
        title: "初始化專案"
        objective: "客觀具體目標描述"
        acceptance_criteria:
          - "建立 pom.xml 或 package.json"
          - "[禁令] 絕對不可以做ＯＯＯ"
          - "[Fallback] API Key 必須設定預設值"
```
